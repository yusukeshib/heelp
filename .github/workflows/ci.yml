name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Swift version
        run: swift --version

      - name: Build
        run: swift build -c release

      - name: Bundle (ad-hoc signed)
        run: make bundle SIGN_IDENTITY=-

      - name: Verify bundle
        run: |
          test -x Heelp.app/Contents/MacOS/Heelp
          plutil -lint Heelp.app/Contents/Info.plist
          codesign --verify --verbose Heelp.app

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Heelp.app
          path: Heelp.app
